VITE_API_BASE_URL=

# When this Vite app is served by the Kroma FastAPI backend, keep API calls on the
# same origin. For local Vite development, use http://127.0.0.1:8000/api/v1 instead.
VITE_KROMA_API_BASE_URL=/api/v1
